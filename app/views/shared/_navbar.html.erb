<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <div class="container-fluid px-4 px-lg-5">
        <%=link_to 'Blog',posts_path, class: "navbar-brand mb-0 h1"%>
        <div class="collapse navbar-collapse">
            <ul class="navbar-nav ms-auto py-4 py-lg-0">

                <% if user_signed_in? %>
                <li class="nav-item">
                    <%= button_to 'New Post', new_post_path, method: "get",class: "btn btn-secondary btn-sm text-white nav-link mx-2"%>
                </li>
                <li class="nav-item">
                    <%= button_to "Log Out", destroy_user_session_path, method: :delete ,class:'nav-link btn btn-secondary text-white btn-sm mx-2' %>
                </li>
                <% else %>
                <li class="nav-item">
                    <%= link_to 'Sign Up',new_user_registration_path , class: "btn btn-secondary btn-sm text-white nav-link mx-2"%>
                </li>
                <li class="nav-item">
                    <%= link_to 'Sign In',new_user_session_path, class: "btn btn-secondary btn-sm text-white nav-link mx-2"%>
                </li>
                <%end%>
            </ul>
        </div>
    </div>
</nav>